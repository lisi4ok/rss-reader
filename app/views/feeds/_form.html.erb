 <div class="row justify-content-center">
<%= form_for(@feed) do |f| %>
  <% if @feed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feed.errors.count, "error") %> prohibited this feed from being saved:</h2>

      <ul>
      <% @feed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-md-7 col-md-offset-5 align-center field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="form-group col-md-7 col-md-offset-5 align-center field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="form-group col-md-7 col-md-offset-5 align-center field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="form-group col-md-7 col-md-offset-5 align-center actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
